/**
 * Lucide React Platform Shim for React Native
 *
 * Maps lucide-react imports to lucide-react-native.
 * The web package uses SVG; this provides React Native SVG equivalents.
 */

export * from 'lucide-react-native';
