/**
 * Auth Guard Middleware - Backend
 * Re-exports canonical implementation from frontend middleware
 * Ensures single source of truth for authentication middleware
 */

export * from '../middleware/authGuard';
