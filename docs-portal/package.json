{
  "name": "bayit-plus-docs-portal",
  "version": "1.0.0",
  "description": "Bayit+ Documentation Portal",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && firebase deploy --only hosting:docs-bayit-plus",
    "deploy:staging": "npm run build && firebase hosting:channel:deploy docs-staging --expires 30d --only docs-bayit-plus"
  },
  "dependencies": {
    "@vue/server-renderer": "^3.5.27",
    "@vueuse/core": "^10.7.2",
    "flexsearch": "^0.7.43",
    "vitepress": "^1.6.4",
    "vue": "^3.5.27"
  },
  "devDependencies": {
    "@types/node": "^20.11.5"
  }
}
