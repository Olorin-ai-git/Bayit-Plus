/**
 * LanguageSelector Component
 * Re-export from shared Glass UI components
 *
 * This file provides backward compatibility for the root-level import.
 * All language selector functionality now uses the Glass design system
 * with support for all 10 languages from @olorin/shared-i18n.
 */

export { GlassLanguageSelector as default } from '../../packages/shared/src/components/ui/GlassLanguageSelector';
