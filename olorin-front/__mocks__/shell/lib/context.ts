const hostname = 'testUrl.test';

export default {
  /**
   * @returns {string} Returns the hostname
   */
  getHostName: () => hostname,
  /**
   * @returns {object} Gets the ui-data-layer context information
   */
  getUIDataLayerContext: () => ({}),
};
