describe('Placeholder Test', () => {
  it.skip('should pass as a placeholder', () => {
    // This is a placeholder test to prevent Cypress from failing
    // when no other spec files are present
    expect(true).to.equal(true);
  });
});
