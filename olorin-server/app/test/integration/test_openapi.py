# from pskopenapi.fastapi_ import get_openapi_spec
# from pskopenapi.testing import assert_spec_complies
#
#
# def test_openapi_spec_complies(app):
#     spec = get_openapi_spec(app)
#     # to allow the test to pass on API spec warnings, set fail_on_warnings=False
#     assert_spec_complies(spec, fail_on_warnings=True)
