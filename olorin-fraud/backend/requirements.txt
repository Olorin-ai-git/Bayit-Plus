# This project uses Poetry for dependency management
# To install all dependencies, please use Poetry instead:
#
# 1. Install Poetry: pip install poetry
# 2. Install dependencies: poetry install
# 3. Run the application: poetry run python -m app.local_server
#
# For more information, see the README.md file
#
# If you need to use pip, generate requirements.txt from Poetry:
# poetry export -f requirements.txt --output requirements.txt --without-hashes

# Minimal dependencies for basic functionality (Poetry is recommended)
poetry>=1.4.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
# PostgreSQL database driver
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0