# App Mode: 'demo' or 'production'
# demo: Uses only mock data, no real API calls
# production: Uses only API data, fails fast, no mocks
VITE_APP_MODE=demo

# API
VITE_API_URL=http://localhost:8000/api/v1

# Stripe (public key)
VITE_STRIPE_PUBLIC_KEY=pk_test_...
