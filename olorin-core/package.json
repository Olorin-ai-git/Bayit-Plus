{
  "name": "@olorin/core",
  "version": "2.0.0",
  "private": true,
  "description": "Olorin Core - Shared infrastructure packages for the Olorin ecosystem",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  }
}
