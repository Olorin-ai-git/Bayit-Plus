/**
 * TV App Theme
 * Re-exports shared theme for backward compatibility
 */

// Re-export all shared theme values
export {
  colors,
  spacing,
  borderRadius,
  fontSize,
  tvFontSize,
  shadows,
  boxShadow,
} from '@bayit/shared/theme';
