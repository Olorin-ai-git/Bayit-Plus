<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">bayit.tv</domain>
        <!--
          Certificate pinning: add the real SHA-256 SPKI hash for api.bayit.tv
          and at least one backup pin before enabling. Derive with:
            openssl s_client -connect api.bayit.tv:443 \
              | openssl x509 -pubkey -noout \
              | openssl pkey -pubreq -outform der \
              | openssl dgst -sha256 -binary | base64
          Store the value in Google Cloud Secret Manager and inject via CI.
        -->
    </domain-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
    </domain-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
