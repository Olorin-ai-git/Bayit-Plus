{
  "name": "@bayit/firebase-config",
  "version": "1.0.0",
  "description": "Shared Firebase configuration for Bayit+ platforms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "firebase",
    "config",
    "bayit",
    "shared"
  ],
  "author": "Bayit+ Team",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
