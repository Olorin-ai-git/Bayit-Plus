# Olorin Ecosystem NPM Configuration
# Authentication for GitHub Packages (private npm registry)

# GitHub Packages registry for @olorin scope
@olorin:registry=https://npm.pkg.github.com

# Authentication token - set via environment variable or npm login
# To authenticate:
#   1. Create a Personal Access Token at https://github.com/settings/tokens
#      - Select scopes: read:packages, write:packages, delete:packages
#   2. Run: npm login --scope=@olorin --registry=https://npm.pkg.github.com
#      - Username: your GitHub username
#      - Password: your Personal Access Token
#      - Email: your email
#   3. Or set: export GITHUB_TOKEN=your_token
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Legacy auth support
legacy-peer-deps=true

# Workspace settings
save-exact=false
# prefer-workspace-packages removed - deprecated in npm 11+

# Performance optimizations
fund=false
audit=false

# Engine compatibility
# Note: superstatic@9.2.0 shows engine warning with Node 24 but works fine
# Firebase team hasn't updated their engine requirements yet
engine-strict=false
