/**
 * Constants for useTVConstantListening Hook
 *
 * VAD (Voice Activity Detection) energy thresholds by sensitivity level.
 */

export const VAD_THRESHOLDS: Record<string, number> = {
  low: 0.025,
  medium: 0.015,
  high: 0.008,
};
